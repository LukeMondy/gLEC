name: gLEC
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - jigsaw
  - jigsawpy
  - matplotlib
  - meshio 
  - mpi4py
  - numpy
  - rasterio
  - scipy
  - pip
  - pip:
    - '--editable=.'  # Install the gLEC package
    - methodtools
